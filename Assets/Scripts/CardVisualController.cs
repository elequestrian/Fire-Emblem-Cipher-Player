using Com.SakuraStudios.FECipherPlayer;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CardVisualController : MonoBehaviour
{
    [SerializeField] private float tapTime = 0.5f;
    [SerializeField] private float flipTime = 0.5f;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    /* Will likely handle card movement in animator again.
    public IEnumerator TapCardObject()
    {
        float tapTimer = 0f;
        
        while (tapTimer < tapTime)
        {
            transform.ro
        }
    }
    */
}
